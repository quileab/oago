$kind: http-request
name: Delete List Price
method: DELETE
url: '{{base_url}}/api/list-prices/{{product_id}}/{{list_id}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
