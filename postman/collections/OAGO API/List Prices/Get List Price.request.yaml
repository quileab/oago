$kind: http-request
name: Get List Price
method: GET
url: '{{base_url}}/api/list-prices/{{product_id}}/{{list_id}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
