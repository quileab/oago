$kind: http-request
name: List Users
method: GET
url: '{{base_url}}/api/users'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
queryParams:
  - key: role
    value: ''
    disabled: true
